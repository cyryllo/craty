# Appka nasłuchuje na 8000, nie domyślnym 80 — zgodnie z docker-compose.yml
# ("8000:8000") i APP_URL w .env. Port >1024, więc kontener może chodzić
# jako zwykły użytkownik hosta (patrz "user:" w docker-compose.yml) bez
# żadnych dodatkowych uprawnień do bindowania portu.
Listen 8000
