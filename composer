#!/usr/bin/env bash
# Uruchamia Composera wewnątrz kontenera deweloperskiego.
# Użycie: ./composer require paczka/nazwa
set -e
docker compose run --rm --no-deps -u "$(id -u):$(id -g)" -e COMPOSER_HOME=/tmp/composer app composer "$@"
